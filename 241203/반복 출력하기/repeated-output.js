const fs = require("fs");
const input = Number(fs.readFileSync(0).toString().trim());

const print = (n) => {
    for (let i = 0; i < n; i++) {
        console.log("12345^&*()_");
    }
}

print(input);